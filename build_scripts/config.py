# config = 'release'
# arch = 'i686'
imageType = 'disk'
imageFS = 'fat32'
# imageSize = '250m'
toolchain = '../CxxToolchain'

# mount method
# guestfs - uses libguestfs, doesn't need sudo
# mount - uses mount, requires sudo
mountMethod = 'guestfs'
